BASELINE_HEAD=baf0a380188093dd829e15a8d10ae68ebb77ad72
BASELINE_SUBJ=baf0a38 2026-08-10 15:10:27 -0400 fix: the paths that EXECUTE were the two that never validated
BASELINE_DATE=2026-08-10 15:10:27 -0400
BASELINE_CAPTURED=2026-08-10 DELIBERATE RE-BASELINE (owner-authorized drift; the FIRST movement since the 2026-07-27 freeze). The oracle was patched and the port matched it in the same breath: wazmrt's two EXECUTE paths (`<module> <export>` and the WASI `_start` command) and its C ABI (`wasm_module_new`) never validated, while its summarize and `.wast` paths did — so it would run an ill-typed module and print a plausible answer. That is what made wasmrt's punch-list item T9a#9 read "the oracle runs this and we refuse it, so our type-checker is wrong" for two releases; it was a false defect, and re-running the same module through the oracle's VALIDATING path showed both runtimes agreeing it is invalid. wasmrt had the identical hole on `wasmrt run` and it was fixed in the same session. Verified at this HEAD: `zig build test` = 5/5 steps, 489/493 tests pass (4 skip) in ALL FOUR modes — Debug, ReleaseSafe, ReleaseFast AND ReleaseSmall (the 07-27 record only claimed the first two; the other two are green and that gap in the record is now closed). wasmtk WASI corpus 376/376 unaffected; the 534-file `.wat` corpus refuses exactly 2 — the byte-identical `39_JstyperMixed` round-trip artifacts, which are genuinely ill-typed and which wasmrt refuses too, so the runtimes now agree exactly (532 valid / 2 invalid). The `.wast` runner is untouched. SCOPE IS UNCHANGED from the freeze: this is a correctness fix to entry points, not a feature move — the only wasmrt-target feature with NO wazmrt oracle is still the tail-call proposal (return_call/return_call_indirect; wazmrt has return_call_ref via function-references but not base tail calls), oracle those against wasmtime + the spec testsuite. THE MONITOR'S JOB IS UNCHANGED: any drift from THIS head means the oracle moved under the in-flight port — investigate and re-baseline deliberately, don't just follow it. (Build note for this machine: `zig build` on the D: drive fails with "error: Unexpected" — a Zig cache/filesystem interaction, not a code problem; build with --cache-dir/--global-cache-dir on C:.)
