[package]
name = "wasmrt-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "wasmrt command-line interface — run/assemble/validate/pin WebAssembly."

# The installed binary is `wasmrt`, not `wasmrt-cli`.
[[bin]]
name = "wasmrt"
path = "src/main.rs"

[dependencies]
wasmrt-core = { workspace = true }

[lints]
workspace = true
